# sports-day
###### Dahboard to register for events in the sports day

Configured with:
- Webpack
- ESLint
- Prettier

## Build

1. Run `yarn`
2. Run `yarn build`

## Run tests

1. Run `yarn `
2. Run `yarn test`

## Running the service

1. Run `yarn`
2. Run `yarn dev`
